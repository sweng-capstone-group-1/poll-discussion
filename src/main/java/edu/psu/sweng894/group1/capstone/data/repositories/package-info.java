/**
 * This are DAO's or DAL objects which will move the "entities" to and from the database.
 * If we use Spring Data JDBC, then Spring will create the concrete implementations of these interfaces for us.
 * If we go with Hibernate or something else, this package wil be removed or changed.
 */
package edu.psu.sweng894.group1.capstone.data.repositories;