package edu.psu.sweng894.group1.capstone.models;

//import edu.psu.sweng894.group1.capstone.models;
import org.junit.Assert;
import org.junit.Test;
import org.mockito.Mock;

public class PollSelectionTest {
    /*

    @Test
    public void testAddVoteTotalVoters() {
        PollSelection selection = new PollSelection("here's an option");
        User user = new User("Rainbow", "Bright");

        selection.addVote(user);
        Assert.assertEquals(user, selection.getTotalVoters()[0]);
    }

    @Test
    public void testGetTotalVotes() {
        PollSelection selection = new PollSelection("here's an option");
        User user = new User("Rainbow", "Bright");

        selection.addVote(user);
        Assert.assertEquals(1, selection.getTotalVotes());
    }

    @Test
    public void testRemoveVote() {
        PollSelection selection = new PollSelection("here's an option");
        User user = new User("Rainbow", "Bright");

        selection.addVote(user);
        Assert.assertEquals(1, selection.getTotalVotes());

        selection.removeVote(user)
        Assert.assertEquals(0, selection.getTotalVotes());
    }
     */
}